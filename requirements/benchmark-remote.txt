# Remote benchmark runner dependencies.
#
# Install this only on CI, Cloudflare Containers, or a rented runner with enough
# disk/RAM. Do not use this profile on a storage-constrained laptop unless you
# are deliberately reproducing the full external campaign locally.
-r benchmark-core.txt

scipy>=1.12
pyarrow>=16.0
ortools>=9.10
python-sat[pblib,aiger]>=1.8.dev0
# tsplib95 removed: it pins networkx~=2.0, which conflicts with networkx>=3.2
# (benchmark-core.txt, required by qiskit/dwave) -> pip ResolutionImpossible.
# The package is unused — TSPLIB instances are fetched from raw URLs and parsed
# directly (see results/official_corpora/tsplib95/). Re-add via `--no-deps` only
# if a maintained, networkx-3-compatible loader is needed.
vrplib>=1.5
pulp>=2.9
mip>=1.15
highspy>=1.7
cvxpy>=1.5
qpsolvers>=4.3
dimod>=0.12
dwave-neal>=0.6
dwave-samplers>=1.3
qiskit>=1.2
qiskit-aer>=0.15
qiskit-optimization>=0.6

# Optional commercial/cloud baselines. Runtime access may require credentials,
# licenses, or cloud account configuration.
gurobipy>=11.0
dwave-system>=1.25
